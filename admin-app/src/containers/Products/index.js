import React from "react";
import Layout from "../../components/Layout";

/**
 * @function Products
 **/

export const Products = (props) => {
  return <Layout sidebar>Products Page Content</Layout>;
};

export default Products;
