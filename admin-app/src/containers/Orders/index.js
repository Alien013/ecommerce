import React from "react";
import Layout from "../../components/Layout";

/**
 * @function Orders
 **/

export const Orders = (props) => {
  return <Layout sidebar>Orders Page Content</Layout>;
};

export default Orders;
